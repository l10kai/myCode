 #include <bits/stdc++.h>
using namespace std;

using i64 = long long;

const char &ln = '\n';

typedef pair<i64, i64> pll;

void solve() {
    i64 n, m;
    cin >> n >> m;

    vector<i64> a(n);

    for(auto &x : a) {
        cin >> x;
    }

    vector<pll> b(m);
    for(auto &[l, r] : b) {
        cin >> l >> r;
    }




}

int main() {
    int t;
    cin >> t;

    while (t--) {
        solve();
    }
    return 0;
}
