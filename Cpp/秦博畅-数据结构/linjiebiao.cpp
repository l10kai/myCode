#include <bits/stdc++.h>

using namespace std;

const int N = 1e3 + 10;
const int inf = 0x3f3f3f3f;
int n, m;

// vector邻接表
vector<vector<int>> g(N);

signed main() {
    cin >> n >> m;  // n为节点数，m为边数

    // dfs(1);
    // bfs(1);
    return 0;
}