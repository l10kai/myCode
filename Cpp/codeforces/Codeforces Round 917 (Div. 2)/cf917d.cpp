
#include <bits/stdc++.h>

using namespace std;

const int N = 200010;

int n,k;
int p[N],q[N];

void solve(){
    cin >> n >> k;
    for(int i = 0 ;i < n; i++) cin >>p[i];
    for(int i = 0; i < k; i++) cin >>q[i];


}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}
